import { Territory } from "@/types/game";

// Empty territories - will be populated when new map is added
export const initialTerritories: Territory[] = [];

export const getMaximallyDistantTerritories = (playerCount: number): string[] => {
  return [];
};
